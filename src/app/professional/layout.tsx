import { ProfessionalShell } from "@/components/ui/layouts";

export default function ProfessionalLayout({ children }: { children: React.ReactNode }) {
  return <ProfessionalShell>{children}</ProfessionalShell>;
}
